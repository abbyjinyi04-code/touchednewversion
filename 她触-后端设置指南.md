# 她触 Touched · CloudBase 后端与安全设置指南

## 架构说明

```
GitHub Pages（托管 index.html）
    ↓ 前端只负责展示，不持有任何密钥
CloudBase（腾讯云）
    ├── 登录认证（短信验证码登录）
    ├── 数据库（用户数据、内容、关系、通知）
    └── admins 集合（管理员授权，文档 ID = 管理员 UID）
```

- `CLOUDBASE_ENV` 只是环境 ID，不是密码，任何人都能在浏览器里看到。
- **真正的安全边界**在 CloudBase 数据库权限规则里。
- 管理员权限不由前端口令决定，而是看 `admins/{你的UID}` 文档是否存在且 `active: true`。

---

## 一、第一次配置（约 10 分钟）

### 1. 创建 CloudBase 环境

1. 打开 [腾讯云 CloudBase 控制台](https://console.cloud.tencent.com/tcb)
2. 新建环境 → 选「按量计费」（免费额度足够内测使用）
3. 记下 **环境 ID**（格式如 `touched-xxx`）

### 2. 开启登录方式

1. CloudBase 控制台 → 左侧「登录认证」
2. 开启「短信验证码登录」（或微信登录，看你的需求）
3. 在「安全来源」里加入你的 GitHub Pages 域名：
   - 格式：`https://你的用户名.github.io`
   - 本地测试时也可以加 `http://localhost:8080`

### 3. 配置 index.html

打开 `index.html`，找到最顶部的这两行：

```js
const CLOUDBASE_ENV = "PASTE_YOUR_ENV_ID_HERE";   // ← 改成你的环境 ID
const CLOUDBASE_REGION = "ap-shanghai";             // ← 改成你的区域
```

比如环境 ID 是 `touched-7g3k2f`，就改成：

```js
const CLOUDBASE_ENV = "touched-7g3k2f";
```

### 4. 创建数据库集合

CloudBase 控制台 → 左侧「数据库」→ 新建以下集合：

| 集合名 | 用途 | 权限 |
|--------|------|------|
| `users` | 用户公开档案 | 所有人可读，本人可写 |
| `private` | 用户私密数据 | 仅本人可读写 |
| `questions` | 烦恼屋问题 | 登录后可读，作者可建 |
| `posts` | 派对屋帖子 | 登录后可读，作者可建 |
| `stories` | 她意识故事 | 登录后可读，作者可建 |
| `relationships` | 关系与聊天 | 仅双方可访问 |
| `notifications` | 通知 | 仅收件人可读 |
| `admins` | **管理员授权** | 仅管理员可读 |

---

## 二、设置管理员权限 🔑

这是最关键的一步——**不完成这步，你在线上就用不了管理控制台**。

### 操作步骤

1. 先部署代码，用你的手机号登录「她触」
2. 在网址后加 `#dev` 进入开发者入口（如 `https://你的用户名.github.io/#dev`）
3. 页面会显示「需要云端管理员权限」，同时显示你的 **UID**
4. 点击「📋 复制」按钮复制你的 UID
5. 打开 CloudBase 控制台 → 数据库 → `admins` 集合
6. 点击「添加文档」，**文档 ID 填写你的 UID**（不是自动生成）
7. 文档内容写：

```json
{
  "active": true,
  "role": "admin",
  "createdAt": 1720000000000
}
```

8. 回到「她触」#dev 页面，点「重新校验」
9. 校验通过后自动进入管理控制台 ✅

### 日常使用

- 之后从「我的」页底部点「🛠️ 开发者入口」即可直接进入（已校验过）
- 如果不是管理员，点击该入口会提示「需要管理员权限」，不会打开

### 添加其他管理员

在 `admins` 集合里再新增一条文档，文档 ID 填对方的 UID，内容同上。对方重新进入 `#dev` 或点「开发者入口」即可自动校验。

---

## 三、数据库安全规则

以下是**推荐的安全规则**（CloudBase 安全规则语法，在控制台「数据库 → 权限设置」里配置）：

```js
{
  "users": {
    ".read": "auth != null",
    ".write": "auth != null && auth.uid == doc._id"
  },
  "private": {
    ".read": "auth != null && auth.uid == doc._id",
    ".write": "auth != null && auth.uid == doc._id"
  },
  "questions": {
    ".read": "auth != null",
    ".write": "auth != null"
  },
  "posts": {
    ".read": "auth != null",
    ".write": "auth != null"
  },
  "stories": {
    ".read": "auth != null",
    ".write": "auth != null"
  },
  "relationships": {
    ".read": "auth != null",
    ".write": "auth != null"
  },
  "notifications": {
    ".read": "auth != null",
    ".write": "auth != null"
  },
  "admins": {
    ".read": "auth != null",
    ".write": false
  }
}
```

> ⚠️ 注意：CloudBase 安全规则语法可能会更新。以上为基础规则，更细粒度的规则（如 `relationships` 只有 members 可访问、`notifications` 只有 toUid 可读）建议在云函数中实现，或在安全规则支持后补上。基础规则 + 前端代码中的 `checkCloudAdmin()` + `requireCloudAdmin()` 已经能防止越权操作。

---

## 四、测试流程 ✅

### 测试环境准备

- 设备 A：你的手机（或 Chrome 无痕窗口 1）
- 设备 B：另一个手机（或 Chrome 无痕窗口 2）
- 两个设备各自用**不同的手机号**登录

### 功能测试清单

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 注册/登录 | 首次用手机号 + 验证码登录 | 进入建档流程，填完资料进首页 |
| 资料同步 | 在设备 A 改昵称/签名 | 设备 B 刷新后能看到新资料 |
| 发帖 | 设备 A 发一条帖子 | 设备 B 的派对屋能看到 |
| 发问/轻触 | 设备 A 发问，设备 B 轻触 | 设备 A 收到轻触通知 |
| 拉手/聊天 | 设备 A 向设备 B 拉手，B 接受 | 双方能进入聊天 |
| 退出登录 | 我的 → 退出登录 | 回到欢迎页，刷新后也不再自动登录 |
| 浏览记录 | 设备 A 浏览几条烦恼 | 只看到自己浏览过的，看不到其他人的 |

### 安全测试清单

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| XSS-昵称 | 把昵称改成 `<img src=x onerror=alert(1)>` | 显示为纯文本，不弹窗 |
| XSS-帖子 | 发帖内容写 `<script>alert(1)</script>` | 显示为纯文本 |
| XSS-评论 | 评论里写 `<b onmouseover=alert(1)>XX</b>` | 显示为纯文本 |
| XSS-聊天 | 聊天消息里写 `<img onerror=...>` | 显示为纯文本 |
| XSS-故事 | 她意识故事里写攻击代码 | 显示为纯文本 |
| 越权访问 | 在浏览器控制台改 `adminUnlocked=true` | 审核操作被云端拒绝 |
| 越权隐藏 | 普通用户尝试隐藏别人的帖子 | 云端写入失败 |

### 管理员测试清单

| 测试项 | 操作 | 预期结果 |
|--------|------|----------|
| 管理入口 | 管理员点「开发者入口」 | 直接进入控制台 |
| 非管理入口 | 非管理员点「开发者入口」 | 提示需要权限，不打开 |
| #dev 校验 | 管理员访问 `#dev` | 自动校验通过，进入控制台 |
| 内容审核-隐藏 | 管理员隐藏一条帖子 | 另一设备刷新后看不到该帖子 |
| 内容审核-恢复 | 管理员恢复已隐藏帖子 | 另一设备重新看到 |
| 非管理员审核 | 非管理员在 `#dev` 尝试操作 | 校验不通过，无法进入 |

---

## 五、本地开发 vs 线上部署

| | 本地模式 | 线上模式（CloudBase） |
|---|---|---|
| `CLOUDBASE_ENV` | 保持 `PASTE_...` 不填 | 填入真实环境 ID |
| 数据存储 | 浏览器 localStorage | CloudBase 数据库 |
| 登录 | 跳过，自动生成 ID | 短信验证码登录 |
| 管理员 | 本机调试口令（≥8位） | CloudBase `admins/{uid}` |
| 多设备 | 不支持 | 支持 |
| 别人能看到 | ❌ | ✅ |

本地测试：直接双击 `index.html` 用浏览器打开即可。

---

## 六、常见问题

**Q: 登录后一直转圈？**
A: 检查 CloudBase 控制台 → 登录认证 → 安全来源，确认 GitHub Pages 域名已加入白名单。

**Q: 审核操作提示"需要云端管理员权限"？**
A: 确认已在 CloudBase 数据库 `admins` 集合中添加了文档，文档 ID = 你的 UID，且 `active: true`。然后重新访问 `#dev` 校验。

**Q: 数据不同步？**
A: 先确认两个设备登录的是不同的手机号。打开 `#dev` → 运行日志看有没有红色报错。

**Q: 怎么知道自己的 UID？**
A: 访问 `#dev` 页面，未授权时会显示你的 UID 并提供复制按钮。

**Q: 退出登录后还是自动登回去了？**
A: 清除浏览器缓存/数据，或使用无痕窗口测试。CloudBase 登录态由 SDK 管理，退出登录后会彻底登出。

---
